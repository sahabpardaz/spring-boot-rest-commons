package ir.sahab.rest.common.apierror;

/**
 * The fields in API responses which represent a problem.
 */
public enum ApiProblemField {
    USER,
    TRACKING_ID,
    API_PATH,
    HTTP_METHOD,
    TIMESTAMP,
    ERROR_CODE,
    EN_MESSAGE,
    FA_MESSAGE
}
